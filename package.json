{
  "name": "palmstory-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PalmStory AI — front-end (TypeScript, built with Vite into static/js).",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
